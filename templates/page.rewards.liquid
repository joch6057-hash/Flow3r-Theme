<div class="page-top">
  <nav class="breadcrumb"><a href="/">Home</a><span class="bc-sep">/</span><span class="bc-cur">Rewards</span></nav>

  <!-- HERO -->
  <div class="rw2-hero">
    <div class="rw2-hero-eyebrow">FLOW3R Rewards</div>
    <h1 class="rw2-hero-h">Earn with every order.</h1>
    <p class="rw2-hero-sub">Join free. Collect points on everything you buy, then turn them into money off your next piece. No catch, no tiers to chase.</p>
    <a href="/account/register" class="rw2-hero-btn">Join Free — Start Earning</a>
  </div>

  <!-- HOW IT WORKS -->
  <div class="rw2-how">
    <div class="rw2-section-label">
      <span class="rw2-tag">How it works</span>
      <h2 class="rw2-section-h">Simple as it sounds.</h2>
    </div>
    <div class="rw2-grid">

      <div class="rw2-card">
        <div class="rw2-card-step">Step 01</div>
        <div class="rw2-card-num">5</div>
        <div class="rw2-card-unit">points per $1 spent</div>
        <h3 class="rw2-card-h">Earn on every order</h3>
        <p class="rw2-card-p">Every dollar you spend earns 5 points. Points are credited automatically once your order ships — nothing to track, nothing to claim.</p>
      </div>

      <div class="rw2-card rw2-card--accent">
        <div class="rw2-card-step">Step 02</div>
        <div class="rw2-card-num">100</div>
        <div class="rw2-card-unit">points = $1 off</div>
        <h3 class="rw2-card-h">Redeem at checkout</h3>
        <p class="rw2-card-p">Turn your points into a discount whenever you're ready. 100 points takes $1 off your order — the more you've earned, the bigger the discount.</p>
      </div>

    </div>
  </div>

  <!-- EXAMPLE / MATH -->
  <div class="rw2-example">
    <div class="rw2-example-inner">
      <div class="rw2-example-label">For example</div>
      <p class="rw2-example-p">Spend <strong>$80</strong> on a water pipe and you earn <strong>400 points</strong> — that's <strong>$4 off</strong> your next order, waiting for you automatically.</p>
    </div>
  </div>

  <!-- JOIN CTA -->
  <div class="rw2-join">
    <h2 class="rw2-join-h">Start earning today.</h2>
    <p class="rw2-join-p">Create a free account and your points start adding up from your very first order.</p>
    <a href="/account/register" class="rw2-join-btn">Create Your Free Account →</a>
    <p class="rw2-join-note">Already have an account? <a href="/account/login">Log in</a> — your points are waiting.</p>
  </div>

</div>

<style>
  .rw2-hero{
    text-align:center;
    padding:72px 24px 64px;
    max-width:720px;
    margin:0 auto;
  }
  .rw2-hero-eyebrow{
    font-size:11px;
    font-weight:600;
    letter-spacing:.18em;
    text-transform:uppercase;
    color:#245034;
    margin-bottom:18px;
  }
  .rw2-hero-h{
    font-size:clamp(38px,6vw,64px);
    font-weight:500;
    letter-spacing:-.03em;
    line-height:1.02;
    color:#0d0d0d;
    margin:0 0 18px;
  }
  .rw2-hero-sub{
    font-size:16px;
    color:#555550;
    line-height:1.65;
    max-width:520px;
    margin:0 auto 30px;
  }
  .rw2-hero-btn{
    display:inline-block;
    background:#245034;
    color:#fff;
    font-size:13px;
    font-weight:600;
    letter-spacing:.06em;
    text-transform:uppercase;
    text-decoration:none;
    padding:16px 34px;
    border-radius:4px;
    transition:background .2s,transform .15s;
  }
  .rw2-hero-btn:hover{ background:#1a3d26; transform:translateY(-1px); }

  .rw2-how{
    max-width:1100px;
    margin:0 auto;
    padding:32px 24px 24px;
    border-top:1px solid #d4d4d4;
  }
  .rw2-section-label{ text-align:center; margin-bottom:40px; }
  .rw2-tag{
    display:inline-block;
    font-size:11px;
    font-weight:600;
    letter-spacing:.16em;
    text-transform:uppercase;
    color:#245034;
    margin-bottom:12px;
  }
  .rw2-section-h{
    font-size:clamp(26px,4vw,38px);
    font-weight:500;
    letter-spacing:-.025em;
    color:#0d0d0d;
    margin:0;
  }
  .rw2-grid{
    display:grid;
    grid-template-columns:1fr;
    gap:20px;
  }
  .rw2-card{
    border:1.5px solid rgba(0,0,0,.1);
    border-radius:10px;
    padding:36px 32px;
    background:#fff;
  }
  .rw2-card--accent{
    background:#245034;
    border-color:#245034;
  }
  .rw2-card-step{
    font-size:10px;
    font-weight:600;
    letter-spacing:.16em;
    text-transform:uppercase;
    color:#999;
    margin-bottom:20px;
  }
  .rw2-card--accent .rw2-card-step{ color:rgba(255,255,255,.5); }
  .rw2-card-num{
    font-size:64px;
    font-weight:600;
    letter-spacing:-.04em;
    line-height:1;
    color:#245034;
  }
  .rw2-card--accent .rw2-card-num{ color:#fff; }
  .rw2-card-unit{
    font-size:13px;
    font-weight:600;
    letter-spacing:.04em;
    text-transform:uppercase;
    color:#555550;
    margin:8px 0 22px;
  }
  .rw2-card--accent .rw2-card-unit{ color:rgba(255,255,255,.6); }
  .rw2-card-h{
    font-size:20px;
    font-weight:600;
    letter-spacing:-.01em;
    color:#0d0d0d;
    margin:0 0 10px;
  }
  .rw2-card--accent .rw2-card-h{ color:#fff; }
  .rw2-card-p{
    font-size:15px;
    color:#555550;
    line-height:1.7;
    margin:0;
  }
  .rw2-card--accent .rw2-card-p{ color:rgba(255,255,255,.78); }

  .rw2-example{
    max-width:1100px;
    margin:0 auto;
    padding:24px 24px 0;
  }
  .rw2-example-inner{
    background:#e8f5ed;
    border-radius:10px;
    padding:32px 36px;
    text-align:center;
  }
  .rw2-example-label{
    font-size:10px;
    font-weight:600;
    letter-spacing:.16em;
    text-transform:uppercase;
    color:#245034;
    margin-bottom:10px;
  }
  .rw2-example-p{
    font-size:clamp(16px,2.4vw,20px);
    color:#0d0d0d;
    line-height:1.6;
    margin:0;
    font-weight:400;
  }
  .rw2-example-p strong{ font-weight:600; color:#245034; }

  .rw2-join{
    max-width:720px;
    margin:0 auto;
    text-align:center;
    padding:64px 24px 88px;
  }
  .rw2-join-h{
    font-size:clamp(26px,4vw,38px);
    font-weight:500;
    letter-spacing:-.025em;
    color:#0d0d0d;
    margin:0 0 12px;
  }
  .rw2-join-p{
    font-size:15px;
    color:#555550;
    line-height:1.65;
    margin:0 0 26px;
  }
  .rw2-join-btn{
    display:inline-block;
    background:#245034;
    color:#fff;
    font-size:13px;
    font-weight:600;
    letter-spacing:.06em;
    text-transform:uppercase;
    text-decoration:none;
    padding:16px 34px;
    border-radius:4px;
    transition:background .2s,transform .15s;
  }
  .rw2-join-btn:hover{ background:#1a3d26; transform:translateY(-1px); }
  .rw2-join-note{
    font-size:14px;
    color:#888;
    margin:20px 0 0;
  }
  .rw2-join-note a{ color:#245034; font-weight:600; text-decoration:none; }
  .rw2-join-note a:hover{ text-decoration:underline; }

  @media (min-width:768px){
    .rw2-grid{ grid-template-columns:1fr 1fr; gap:24px; }
  }
  @media (max-width:600px){
    .rw2-hero{ padding:52px 20px 48px; }
    .rw2-card{ padding:30px 24px; }
    .rw2-example-inner{ padding:28px 24px; }
  }
</style>