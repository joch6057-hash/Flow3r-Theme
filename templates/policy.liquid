<div class="page-top">
  <nav class="breadcrumb"><a href="/">Home</a><span class="bc-sep">/</span><span class="bc-cur">{{ page.title }}</span></nav>

  <div style="max-width:760px;margin:0 auto;padding:60px 44px 80px">
    <h1 style="font-size:38px;font-weight:800;letter-spacing:-.02em;margin-bottom:32px;color:var(--bk)">{{ page.title }}</h1>
    <div style="font-size:16px;color:var(--g2);line-height:1.9" class="rte">
      {{ page.content }}
    </div>
  </div>
</div>